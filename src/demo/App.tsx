export function App() {
  return (
    <main className="app-shell">
      <section className="empty-state" aria-labelledby="page-title">
        <div className="mark" aria-hidden="true">
          <span />
          <span />
          <span />
        </div>
        <h1 id="page-title">WebGL Learning</h1>
        <p>项目骨架已就绪，可以从第一个 WebGL 示例开始。</p>
        <code>src/core</code>
      </section>
    </main>
  );
}
